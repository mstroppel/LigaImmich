# Example environment for running the LigaImmich worker in Docker.
# Copy to `.env` and adjust. Use with `docker run --env-file .env ...`.

# --- Immich API connection (required) ---
IMMICH_BASE_URL=https://immich.example.com/api
IMMICH_API_KEY=replace-me

# --- Scheduler (optional; shown with defaults) ---
# SCHEDULER_TIMEZONE=Europe/Berlin
# SCHEDULER_TAG_ASSETS_BY_CLUB_CRON=0 15 3 * * *
# SCHEDULER_TAG_ASSETS_BY_CLUB_ENABLED=true
# SCHEDULER_TAG_ASSETS_BY_FOLDER_STRUCTURE_CRON=0 15 4 * * *
# SCHEDULER_TAG_ASSETS_BY_FOLDER_STRUCTURE_ENABLED=true
# SCHEDULER_TAG_ASSETS_BY_YEAR_CRON=0 15 3 * * *
# SCHEDULER_TAG_ASSETS_BY_YEAR_ENABLED=true
